import { createTheme } from "@mui/material/styles";

const theme=createTheme({
  palette:{
    primary:{
        main:'#242C39'
    }
  },
  typography: {
    fontFamily: 'Niramit',
  },
})

export default theme